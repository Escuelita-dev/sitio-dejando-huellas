const config = {
    strapiBaseUri: process.env.API_URL || "https://strapi.dejando-huellas.escuelita.dev",
    strapiCdnBaseUri: process.env.API_URL || "https://strapi.dejando-huellas.escuelita.dev",
}

export default config